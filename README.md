# Knaq Take-Home

## Quick Start (Docker)
docker compose up --build
# web: http://localhost:3000
# api: http://localhost:8000

## Local Dev
cd api && npm install && npm run dev
cd web && npm install && npm run dev

## Default user
token-sarah → Sarah Chen, Brookfield Properties (made it changeable in ap)